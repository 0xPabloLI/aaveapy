/**
 * Canonical public API base URLs for Node scripts (and cross-reference for docs).
 * Frontend runtime default when `VITE_API_BASE_URL` is unset: `src/lib/apiBase.ts`
 * (keep in sync with DEFAULT_STAGING_API_BASE).
 */
export const DEFAULT_STAGING_API_BASE = 'https://staging-api.aaveapy.com/api';
export const DEFAULT_PRODUCTION_API_BASE = 'https://api.aaveapy.com/api';
