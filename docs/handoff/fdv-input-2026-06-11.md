# FDV Input Handoff — 2026-06-11

## 本 Session 已完成

### 修改
1. **FDV 输入失焦修复** — `InkAprCalculator.tsx#L954`
   - `<FullLayout />` → `FullLayout()`
   - 根因：React 组件每次 re-render 重建 DOM，导致 input 失焦

2. **FDV 输入迁移到 `useDebouncedInput`** — `InkAprCalculator.tsx#L277-L287`
   - 统一使用 `useDebouncedInput` hook，300ms debounce
   - 和其他 input 行为一致

3. **FDV 输入限制小数点后 2 位** — 3 个文件
   - `src/lib/numberFormat.ts`: `sanitizeNumberInput` 新增 `maxDecimalPlaces` 参数
   - `src/hooks/useDebouncedInput.ts`: `UseDebouncedInputParams` 新增 `maxDecimalPlaces` 选项
   - `src/components/dashboard/InkAprCalculator.tsx`: FDV input 传入 `maxDecimalPlaces: 2`

4. **滑块与输入同步** — `sliderActiveRef` 守卫防止 blur 事件覆盖滑块更新

5. **E2E 测试** — `e2e/fdv-continuous-input.spec.ts`
   - 连续输入不失焦
   - 快速输入无丢字符
   - 小数输入正常
   - 慢速输入（debounce 在两次按键之间触发）正常
   - 小数截断到 2 位（`1.234` → `1.23`）
   - 恰好 2 位小数不截断（`3.14` → `3.14`）

6. **Code Review 修复** — commit `2a74e24c`
   - `InkAprCalculator.tsx`: `Math.round(x * 100) / 100` → `Number(x.toFixed(2))`（浮点精度）
   - `InkAprCalculator.tsx`: 添加 `FullLayout()` 函数调用模式注释
   - `useDebouncedInput.ts`: 添加 `handleBlur`/`handleKeyDown` 注释说明 maxDecimalPlaces 不重复应用
   - `docs/handoff/AAV-749-simulation-regression-handoff.md`: 更新修复状态和经验教训

### CI 状态
- lint: ✅ (已有错误非本 session 引入)
- test: ✅ (已有失败非本 session 引入)
- build: ✅
- tsc --noEmit: ✅

---

## 待做 (Backlog)

### AAV-798: MAX_FDV 动态化 ✅ (2026-06-12 已决策，待实现)
**优先级**: Low  
**描述**: `MAX_FDV = 115.8` 硬编码在 `InkAprCalculator.tsx`。应从 CoinGecko 动态获取 FDV 作为 slider 端点。

**设计决策** (grill-with-docs, 2026-06-12):
- **MAX_FDV 来源**: `max(referencePointsWithLiveFdv.map(p => p.fdv))`，即所有参考点中成功获取到 live FDV 的最大值
- **Fallback**: 没有任何 live 数据时（全 null），fallback = `200`
- **统一入口**: 输入限制和滑块端点始终同步，都从同一个 `MAX_FDV` 取值
- **不区分全量/部分**: 只要有至少一个 live FDV 就用最大值，不判断"全不全"

**关键位置**:
- `src/components/dashboard/InkAprCalculator.tsx#L29`: `const MAX_FDV = 115.8;` → 删除，改为从 `referencePointsWithLiveFdv` 计算
- `src/components/dashboard/InkAprCalculator.tsx#L271`: `updateFromFdv` 使用 `Math.min(MAX_FDV, ...)`
- `src/components/dashboard/InkAprCalculator.tsx#L304`: `handleKeyDown` 使用 `Math.min(MAX_FDV, ...)`
- `aria-valuemax={MAX_FDV}` 两处（桌面端 + 移动端滑块）

**注意**: `useCoingeckoFdv()` 已在组件中调用，`referencePointsWithLiveFdv` 已使用 live FDV 更新参考点。只需将 `MAX_FDV` 常量改为从 `referencePointsWithLiveFdv` 派生即可。

### AAV-799: FDV 输入测试补充
**优先级**: Low  
**描述**: 补充 cursor 位置、小数输入、滑块同步的单元测试。

**已覆盖**: E2E 测试（`e2e/fdv-continuous-input.spec.ts`）已覆盖连续输入、快速输入、小数输入。  
**待补充**: 单元测试（`InkAprCalculator.test.tsx`）中 cursor 位置和滑块同步的边界用例。

---

## FDV vs Simulation Input 差异参考

| | FDV input | Simulation input |
|---|---|---|
| 文件 | `InkAprCalculator.tsx` | `ScenarioControls.tsx` |
| hook | `useDebouncedInput` | `useDebouncedInput` |
| debounce | 300ms | 300ms |
| value 来源 | 直接 `value` prop（派生自 `rateInput`） | `externalSupplyValue` state（初始 `undefined`） |
| 外部校正 | 无（clamp 在 onCommit 内） | `ref.setSupplyInput` 通过 `useImperativeHandle` 暴露 |
| 滑块 | `sliderActiveRef` 守卫 | 无 |
| UI 组件 | shadcn `Input` | 原生 `<input>` |
| 小数限制 | 2 位 | 无 |
| 原因 | 有界值 0-115.8，不需外部校正 | 需要外部命令式回写校正值 |

### 差异解释

**1. 直接 value prop vs externalSupplyValue**

- **FDV**: `value` 始终派生自 `currentFdvBillions`（即 `rateInput`），始终是 controlled 模式。FDV 的「真实值」在父组件 `rateInput` 中，`useDebouncedInput` 只是显示和编辑层。
- **Simulation**: `externalSupplyValue` 初始为 `undefined`。`undefined` 时 hook 进入 uncontrolled 模式（用户自由输入）。当外部需要校正时（如点击 max 按钮、shared scenario 切换），`ref.setSupplyInput` 设值后变为 controlled，下次 onCommit 时 `setExternalSupplyValue(undefined)` 回到 uncontrolled。
- **为什么有这个区别**: FDV 值的来源是确定的（rateInput → currentFdvBillions），不需要外部校正。Simulation 可能被各种外部操作（max、shared scenario、mode switch）命令式修改，需要 `useImperativeHandle` 暴露 `setSupplyInput`/`setBorrowInput`。

**2. shadcn Input vs 原生 input**

shadcn `Input` 本质是原生 `<input>` 的封装，加了 Tailwind 样式 tokens（border、focus ring、padding 等）。两者功能完全等价，都接收 `ref`、`value`、`onChange`、`onBlur`、`onFocus`、`onKeyDown` 等标准 props。FDV 用 shadcn 是因为需要 `disableSurface` 和 `cnDsInputNeutralWell` 等设计系统样式；Simulation 用原生是历史原因，切换无功能差异。

**3. 滑块守卫 (sliderActiveRef)**

只有 FDV 有 slider。当用户拖动滑块时，mousedown 触发 `updateFromFdv` → `setRateInput` → `currentFdvBillions` 变化 → `value` prop 变化 → `useDebouncedInput` 内部 sync 到 `displayValue`。但拖动期间 `handleBlur` 也会触发（input 失去焦点），此时 `doCommit` 会覆盖 slider 的更新。`sliderActiveRef` 在 onCommit 开头检查，如果 slider 正在活跃则跳过 commit。

**4. 统一入口**

所有数值输入统一使用 `useDebouncedInput` hook，共享以下逻辑：
- `sanitizeNumberInput`（CJK 全角小数点归一化、非法字符过滤）
- `formatNumberInput`（实时千分位格式化）
- `computeCursorAfterFormat`（格式化后光标位置计算）
- `pendingCursorRef` + `useLayoutEffect`（re-render 后恢复光标）
- `maxDecimalPlaces`（可选，FDV 用 2 位）

**结论**: 核心逻辑已统一到 `useDebouncedInput`。FDV 和 Simulation 的差异由不同业务需求驱动（有界 slider vs 无限输入 + 外部校正），不是代码重复，不需要进一步统一。

---

## 临时文件清理

以下 E2E 测试文件为本 session 诊断用，可删除：
- `e2e/fdv-slow-typing.spec.ts`
- `e2e/fdv-dom-mutation.spec.ts`