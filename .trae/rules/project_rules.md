# Project Rules

## Mandatory Skill Bootstrap (技能自举 — 每次对话必须执行)

Before ANY other action, invoke the `Skill` tool with name `using-superpowers`, then follow instructions exactly.

## 所有其他规则

以 `AGENTS.md` 为唯一来源，不在此重复。包括：验证门禁、Stash 安全、Session Boundary、Mobile Layout、Canary/Hooks、High-Risk Areas 等。</think>

<｜DSML｜parameter name
